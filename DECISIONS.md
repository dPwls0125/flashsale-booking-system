# DECISIONS: 기술적 결정 사항 및 전략

본 문서는 선착순 타임세일 시스템을 구현하며 내린 주요 기술적 결정과 그 근거를 기록합니다.

## 1. 조회(Checkout)와 예약(Booking)의 전략 분리
사용자가 상품 정보를 확인하는 시점과 실제 구매 버튼을 누르는 시점은 부하의 성격이 완전히 다르므로 이를 분리하여 대응했습니다.

- **조회(Checkout) - 가용성 중심**:
  - **결정**: 로컬 캐시(Caffeine)를 적극 활용하여 DB 접근을 최소화하고, 모든 사용자에게 즉각적인 응답을 제공합니다.
  - **근거**: 조회 단계에서 재고 예약이나 차단을 걸면 사용자의 진입 자체가 막혀 비즈니스 기회를 상실하게 됩니다. 따라서 조회는 최대한 빠르고 가볍게 처리합니다.
- **예약(Booking) - 정합성/차단 중심**:
  - **결정**: Local -> Redis -> DB로 이어지는 단계별 필터링을 통해 시스템 붕괴를 막고 정확히 10건만 선착순 처리합니다.
  - **근거**: 00시 정각의 폭발적인 트래픽은 이 구간에서 발생하므로, 여기서 시스템 리소스를 효율적으로 배분하고 초과 판매를 막는 것이 핵심입니다.

## 2. 재고 관리: 단계별 트래픽 필터링을 통한 DB 부하 차단
초당 수천 건 이상의 트래픽이 DB로 직접 몰릴 경우 커넥션 고갈 및 성능 저하가 발생합니다. 이를 방지하기 위해 요청이 DB에 도달하기 전 단계적으로 걸러내도록 설계했습니다.

- **애플리케이션 메모리 카운터 (1차 필터링)**: 
  - **결정**: `AtomicInteger`를 사용하여 애플리케이션 메모리에서 재고 수량만큼만 통과시키고 초과 요청은 즉시 차단합니다. 카운터는 애플리케이션 시작 시 DB 재고 수량으로 초기화되며, 요청마다 1씩 차감합니다.
  - **근거**: 네트워크 통신이 필요한 Redis나 DB에 접근하기 전, 메모리 수준에서 1차적으로 걸러내어 전체 시스템의 가용성을 보호합니다.
- **Redis 원자적 차감 (2차 필터링)**:
  - **결정**: Redis의 `DECR` 연산을 사용하여 여러 서버 분산 환경에서도 단일 재고 수량을 정확히 관리합니다.
  - **근거**: Redis는 싱글 스레드 방식으로 동작하여 별도의 분산 락(Lock) 없이도 동시성 이슈를 원자적으로 해결할 수 있으며, 응답 속도가 매우 빠릅니다.
- **DB 최종 정합성 확인**:
  - **결정**: `UPDATE ... SET remaining = remaining - 1 WHERE remaining > 0` 쿼리를 사용합니다.
  - **근거**: 캐시(Redis)와 DB 간의 일시적인 수량 차이가 발생하더라도, 실제 판매 수량이 재고를 초과하는 '초과 판매'를 물리적으로 차단하는 최후의 방어선입니다.

## 3. 결제-예약 정합성: 단계별 실패 시 자동 복구 로직 구현
외부 PG 결제와 내부 DB 저장은 하나의 트랜잭션으로 묶을 수 없습니다. 결제는 성공했는데 DB 저장이 실패하는 상황에 대응하기 위해 수동 복구 로직을 구현했습니다.

- **결정**: 예약 처리 과정 중 어느 단계에서든 예외가 발생하면, `BookingFacadeService`가 이미 완료된 이전 단계들을 역순으로 되돌리는 로직(Compensation)을 수행합니다.
- **근거**: PG 결제 승인 후 DB 저장 실패 시, 명시적으로 결제 취소 API를 호출하고 Redis 재고를 다시 증가시키는 복구 과정이 필수적입니다. 이를 통해 데이터 불일치를 방지합니다.

## 4. 중복 요청 및 중복 구매 차단
- **중복 구매 방지**: Redis Set(`SADD purchased:product:{id}`)에 memberId를 추가하여 한 회원이 여러 번 선착순에 성공하는 것을 차단합니다. DB의 `(member_id, product_id)` 복합 유니크 제약 조건이 Redis 장애 시 최종 안전망 역할을 합니다.
- **중복 요청(멱등성) 처리**: `IdempotencyService`가 Redis `SETNX`로 `idempotency:{key}` 키를 세팅하여 동일 요청의 중복 처리를 1차 차단합니다. Redis 장애 시에는 DB의 `idempotency_key` 유니크 제약 조건이 fallback으로 동작합니다. 처리 성공 시 상태를 `COMPLETED`로 변경하고, 롤백 시 키를 삭제하여 클라이언트의 재시도를 허용합니다.

## 5. Redis 고가용성(HA) 구성: Sentinel vs Cluster

Redis 장애 시 서비스 중단을 막기 위해 HA 구성이 필요했고, 두 가지 방안을 검토했습니다.

| 항목 | Redis Sentinel | Redis Cluster |
|---|---|---|
| 구성 | Master 1 + Replica N + Sentinel N | Master 최소 3 + Replica 각 1 권장 |
| 데이터 분산 | 없음 (단일 Master에 집중) | 16,384 슬롯으로 샤딩 |
| Failover | Sentinel 프로세스가 감지 후 Replica 승격 | 내장 자동 Failover |
| 운영 복잡도 | 낮음 | 높음 (슬롯 리밸런싱 등) |
| 적합한 규모 | 소규모 데이터, 단일 Master 허용 | 대규모 데이터, 수평 확장 필요 시 |

- **결정**: Redis Sentinel 채택 (Master 1 + Replica 2 + Sentinel 3 구성)
- **근거**: 본 시스템은 상품 수가 적고(1개 한정 상품) 데이터 샤딩이 불필요합니다. Cluster는 최소 Master 3개가 필요해 구성 비용 대비 실익이 없습니다. Sentinel만으로도 Master 장애 시 자동 Failover를 보장할 수 있으며, 구성과 운영이 단순하여 과제 범위에 적합하다고 판단했습니다.

## 6. 인프라 장애 대응 전략
- **Redis 장애 시 (Fallback)**: Redis 연결 실패 시 예외를 무시하고 요청을 DB로 직접 전달합니다.
- **근거**: 서비스 전체가 중단되는 것보다 성능이 저하된 상태로 서비스를 유지하는 것이 낫다고 판단했습니다. DB 레이어의 원자적 업데이트가 최종 정합성을 보장하고 있으므로 가능합니다.
